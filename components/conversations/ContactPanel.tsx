'use client'

// Phase 2 (Task 5) implements the full GHL contact panel: contact info + the
// person's opportunities. Stub renders nothing until then.
export function ContactPanel({ conversationId }: { conversationId: string | null }) {
  if (!conversationId) return null
  return null
}
